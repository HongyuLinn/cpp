// Multi.cpp : Este archivo contiene la función "main". La ejecución del programa comienza y termina ahí.
//

#include <iostream>
#include <string>

using namespace std;

int main()
{

                                                                            // prova 1

    /*int number_of_pods, peas_per_pod, total_peas;

    cout << "Press return after entering a number.\n";  

    cout << "Enter the number of pods: ";   

    cin >> number_of_pods; 

    cout << "Enter the number of peas in a pod: ";
    cin >> peas_per_pod;
    
    total_peas = number_of_pods * peas_per_pod;
    cout << "If you have ";   
    cout << number_of_pods;   
    cout << " pea pods\n";        
    cout << "and ";   
    cout << peas_per_pod;    
    cout << " peas in each pod, then\n";
    cout << "you have ";   
    cout << total_peas;  
    cout << " peas in all the pods.\n";
    return 0; */


                                                                             //prova 2
    
    
    /*int candy_bars;
    double ounces_each, total_weight;

    cout << "Then press return.\n";

    cout << "Candy bars: ";
    cin >> candy_bars;

    cout << "ounces each: ";
    cin >> ounces_each;

    total_weight = candy_bars * ounces_each;

    cout << "total weight is ";
    cout << total_weight << endl;
    cout << "Gordo de mierda";
    return 0;*/

                                                                                 //prova 3

    /*char symbol1, symbol2, symbol3;
   	cout << "Enter two initials, without any periods:\n";
    cin >> symbol1 >> symbol2;
    cout << "The two initials are:\n";
    cout << symbol1 << symbol2 << endl;
    cout << "Once more with a space:\n";
  	symbol3 = ' ';
    cout << symbol1 << symbol3 << symbol2 << endl;
    cout << "That's all.";
    return 0;*/



                                                                                 //prova 4
    /*string day;
    day = "dilluns";
    cout << "Digues un dia de la setmana: ";
    cin >> day;
    cout << day;

    return 0;*/


                                                                          // prova 5

   // 1. Doneu la declaració per a dues variables anomenades peus i polzades.Ambdós  són de tipus int i ambdues s'han d'inicialitzar a zero en el  declaració.Utilitzeu les dues alternatives d'inicialització.

    int peus, polzades;
    peus = 0;
    polzades = 0;

    cout << "peus: ";
    cout << peus << endl;

    cout << "polzades: ";
    cout << polzades; 
    
  
   // 2. Escriu un programa que contingui sentències que generin el valor de cinc o sis variables que s'han declarat, però no s'han inicialitzat.Compilar i executar  el programa.Quina és la sortida ? Explicar.

    int var1, var2, var3, var4, var5, var6;

    var2 = 2;

    cout << var2;

    return 0;

   // 3. Introdueix un valor en ftemp(Fahrenheit) i convertir en ctemp(Celsius).A continuació, imprimiu el  Resultats. (Consell : la fórmula de conversió inversa és ctemp = (ftemp − 32) / 1.8.)
   
    double ftemp, ctemp;

    cout << "Digues la ftemp: ";
    cin >> ftemp;
    ctemp = (ftemp - 32) / 1.8;
    cout << "La ctemp es: ";
    cout << ctemp;
    return 0;



}

// Ejecutar programa: Ctrl + F5 o menú Depurar > Iniciar sin depurar
// Depurar programa: F5 o menú Depurar > Iniciar depuración

// Sugerencias para primeros pasos: 1. Use la ventana del Explorador de soluciones para agregar y administrar archivos
//   2. Use la ventana de Team Explorer para conectar con el control de código fuente
//   3. Use la ventana de salida para ver la salida de compilación y otros mensajes
//   4. Use la ventana Lista de errores para ver los errores
//   5. Vaya a Proyecto > Agregar nuevo elemento para crear nuevos archivos de código, o a Proyecto > Agregar elemento existente para agregar archivos de código existentes al proyecto
//   6. En el futuro, para volver a abrir este proyecto, vaya a Archivo > Abrir > Proyecto y seleccione el archivo .sln